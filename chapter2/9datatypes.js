  var n = 5
  var f = 4.5
  var nn = Number(76.56)      //Number datatypes
  var nnn =  Number(67)

  var string = 'javascript' //this is called literal(the way we put the string into a variable)
  var str1 = 'shifat'
  var str2 =  "Shifat"
  var str3 = String("44")
  var str4 = String('My name is Shifat')       //String Constructor
  var str5 =  String('java')

  var b1 = true
  var b2 = false
  var b3 = Boolean(b1)         //Boolean constructor

  var abc           //Undefined datatypes
  var xyz = null      // Null datatypes


  var hex = 0Xffe   // Hexadecimal- just use 0X before the Hexadecimal number.

  var oct = 0467    // Octal-  just use 0 before the decimal number.

  console.log(str4, str5);
  console.log(string, str1, str2, str3);

  console.log(nn);
  console.log(Number.parseFloat(nnn));
  console.log(Number.parseInt(nn))
  console.log(Number.MAX_VALUE)
  console.log(Number.MIN_SAFE_INTEGER);
  console.log(1 / 0);  //infinite
  console.log("abc" / 4)  //Nan(not a Number)

  console.log(b1, b2, b3);

  console.log(abc, xyz);

  console.log(hex)
  console.log(oct);
  //====================================//

  // datatypes of javascript//

  //1. primitive
  // Number
  // 10, 34, 56.454
  // string
  // 'this is string' "this is string"
  // boolean
  // true false


  //2. object
  // array
  // object
  // function
